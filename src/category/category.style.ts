import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  name: {
    fontSize: 20,
    color: "black",
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default styles;
